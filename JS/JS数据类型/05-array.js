let arr = [1, 3, 4]
console.log(person.age) // 20
console.log(person['name']) // ls
console.log(person.sex) // undefined


/* 
  ## 数组是特殊的对象
    - 中括号中设置的属性值，它的属性名是默认生成的数字，从零开始递增
    - 天生默认一个属性名 length， 存储数组的长度
*/